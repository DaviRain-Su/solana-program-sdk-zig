# Story: v3.2.48 Anchor Token/Mint Constraint Runtime

> Validate token and mint constraints against SPL token state at runtime.

## Goals

- Enforce `token::mint` and `token::authority` using token account state.
- Enforce `mint::authority`, `mint::freeze_authority`, and `mint::decimals` using mint state.
- Validate token/mint program owner constraints (default or custom program).

## Acceptance Criteria

### anchor/src/account.zig

- [x] Token constraints check token account state and program owner.
- [x] Mint constraints check mint state and program owner.
- [x] Default token program owner is enforced when custom program not set.
- [x] Tests cover token and mint constraint validation.

### Documentation

- [x] Update `anchor/README.md` and `docs/ANCHOR_COMPATIBILITY.md`.

### Tests

- [x] `../solana-zig/zig build test --summary all` passes in `anchor/`.

## Completion Status

- Start date: 2026-01-13
- Completion date: 2026-01-13
- Status: ✅ Completed
