# Story: v3.1.4 Root IDL Default Output

> Default root IDL build output to `idl/` with configurable path options.

## Goals

- Default root `zig build idl` output to `idl/` using program metadata/type name.
- Provide configurable `idl-output-dir` and `idl-output` overrides.
- Sync documentation and acceptance tests.

## Acceptance Criteria

### build.zig

- [x] Root `idl` step supports `idl-output-dir` and `idl-output`.
- [x] Default output path uses `idl/` when no override is provided.

### anchor/src/idl_cli.zig

- [x] Derives default file name from program metadata/type name.
- [x] Writes to `idl/` when `idl-output` is unset.

### Documentation

- [x] README updated with new IDL output defaults and flags.
- [x] ANCHOR_COMPATIBILITY updated with root IDL output notes.
- [x] CHANGELOG entry added.
- [x] ROADMAP entry added.

### Tests

- [x] `zig build idl` generates output in `idl/`.

## Completion Status

- Start date: 2026-01-12
- Completion date: 2026-01-12
- Status: ✅ Completed
