//! SPL Associated Token Account instruction builders.
//!
//! The real allocation-free builders land in the follow-up
//! `spl-ata-instruction-builders` feature. This foundation file keeps
//! the public module export stable for `@import("spl_ata")`.
